#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: A program to print all single dihit numbers
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
